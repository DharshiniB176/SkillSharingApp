export default {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: 'var(--primary)',
        primaryHover: 'var(--primary-hover)',

        bg: 'var(--bg-main)',
        card: 'var(--bg-card)',
        input: 'var(--bg-input)',

        text: 'var(--text-main)',
        muted: 'var(--text-muted)',
        inverse: 'var(--text-inverse)',

        border: 'var(--border-color)',

        success: 'var(--success)',
        error: 'var(--error)',
        warning: 'var(--warning)',
      }
    }
  }
}
